This is a self-invented AI Training and Inference framework developed by C++ and constructed using cmake, which is based on dynamic computation graph and auto differentiation, providing several interfaces allowing users to construct MLP neural networks and resnet, also allowing batch training and multi-thread acceleration on CPU, which would be extended to more generalized functions including CNN and transformers in later versions.
