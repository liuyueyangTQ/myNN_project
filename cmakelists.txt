cmake_minimum_required(VERSION 3.10)
project(Metrix_test)

# 设置C++标准
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
# 1. 先设置构建类型为 Debug（关键！CMake 会自动适配 Debug 模式的默认编译选项）
# set(CMAKE_BUILD_TYPE Debug CACHE STRING "Build type" FORCE)
# 🔥 强制开启 Release 模式
set(CMAKE_BUILD_TYPE "Release" CACHE STRING "Build type" FORCE)
# 🔥 开启最高级优化
set(CMAKE_CXX_FLAGS_RELEASE "${CMAKE_CXX_FLAGS_RELEASE} -O3 -march=native")

# 2. 强制为 C/C++ 设置 Debug 模式的编译选项（覆盖默认，确保 -g -O0 生效）
# 针对 C++ 文件
set(CMAKE_CXX_FLAGS_DEBUG "${CMAKE_CXX_FLAGS_DEBUG} -g -O0")
# 如果项目包含 C 文件，补充这行
set(CMAKE_C_FLAGS_DEBUG "${CMAKE_C_FLAGS_DEBUG} -g -O0")

# 对于MinGW，需要设置正确的编译器标志
if(MINGW)
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -static-libgcc -static-libstdc++")
endif()
# 编译阶段：添加-flto
if(CMAKE_CXX_COMPILER_ID MATCHES "GNU|Clang")
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -O3 -flto -march=native")
    # 链接阶段：添加-flto
    set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} -flto")
    set(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} -flto")
endif()

# 引入封装的函数文件
include(${CMAKE_SOURCE_DIR}/cmake/nnhelper.cmake)

set(CORE_DIR "${CMAKE_SOURCE_DIR}/src/core")
message(STATUS "CORE_DIR: ${CORE_DIR}")


add_nn_executable(metrix_test 
    MAIN_DIR    
        ${CORE_DIR}
    SRCS
        ${CORE_DIR}/test/metrix_test.cpp
        ${CORE_DIR}/math/metrix.cpp
        ${CORE_DIR}/utils/buffer_util.cpp
    # 可选：追加自定义宏
    DEFINES
        USE_DEBUG 
    # 可选：追加自定义包含目录
    INCLUDES
)

add_nn_executable(tensor_test 
    MAIN_DIR    
        ${CORE_DIR}
    SRCS
        ${CORE_DIR}/test/tensor_test.cpp
        ${CORE_DIR}/math/metrix.cpp
        ${CORE_DIR}/tensor/tensor.cpp
        ${CORE_DIR}/utils/buffer_util.cpp
    # 可选：追加自定义宏
    DEFINES
        USE_DEBUG 
    # 可选：追加自定义包含目录
    INCLUDES
)

add_nn_executable(nn_test 
    MAIN_DIR    
        ${CORE_DIR}
    SRCS
        ${CORE_DIR}/test/tensor_test.cpp
        ${CORE_DIR}/math/metrix.cpp
        ${CORE_DIR}/tensor/tensor.cpp
        ${CORE_DIR}/utils/buffer_util.cpp
    # 可选：追加自定义宏
    DEFINES
        USE_DEBUG 
        USE_THREAD
    # 可选：追加自定义包含目录
    INCLUDES
)
add_nn_executable(thread_test 
    MAIN_DIR    
        ${CORE_DIR}
    SRCS
        ${CORE_DIR}/test/tensor_test.cpp
        ${CORE_DIR}/math/metrix.cpp
        ${CORE_DIR}/tensor/tensor.cpp
        ${CORE_DIR}/utils/buffer_util.cpp
    # 可选：追加自定义宏
    DEFINES
        USE_DEBUG 
        USE_THREAD
    # 可选：追加自定义包含目录
    INCLUDES
)

add_nn_executable(dtensor_test
    MAIN_DIR    
        ${CORE_DIR}
    SRCS
        ${CORE_DIR}/test/dtensor_test.cpp
        ${CORE_DIR}/math/metrix.cpp
        ${CORE_DIR}/tensor/dtensor.cpp
        ${CORE_DIR}/tensor/ops.cpp
    # 可选：追加自定义宏
    DEFINES
        USE_DEBUG 
        USE_THREAD
        DEBUG_ASSERT_H
    # 可选：追加自定义包含目录
    INCLUDES
)

add_nn_executable(dnn_test 
    MAIN_DIR    
        ${CORE_DIR}
    SRCS
        ${CORE_DIR}/test/dnn_test.cpp
        ${CORE_DIR}/math/metrix.cpp
        ${CORE_DIR}/tensor/dtensor.cpp
        ${CORE_DIR}/tensor/ops.cpp
        ${CORE_DIR}/nn/nn.cpp
        ${CORE_DIR}/thread/threadpool.cpp
    # 可选：追加自定义宏
    DEFINES
        USE_DEBUG 
        USE_THREAD
        DEBUG_ASSERT_H
    # 可选：追加自定义包含目录
    INCLUDES
)


add_nn_executable(draw_nn_test 
    MAIN_DIR    
        ${CORE_DIR}
    SRCS
        ${CORE_DIR}/test/draw_nn_test.cpp
        ${CORE_DIR}/math/metrix.cpp
        ${CORE_DIR}/tensor/tensor.cpp
        ${CORE_DIR}/nn/model.cpp
        ${CORE_DIR}/thread/threadpool.cpp
        ${CORE_DIR}/utils/buffer_util.cpp

        ${CMAKE_SOURCE_DIR}/src/visual/buttons.cpp
    # 可选：追加自定义宏
    DEFINES
        USE_DEBUG 
        # USE_THREAD
    # 可选：追加自定义包含目录
    INCLUDES
        ${CMAKE_SOURCE_DIR}/inc
)
include(${CMAKE_SOURCE_DIR}/cmake/openGLhelper.cmake)
add_opengl_support(draw_nn_test)

# add_subdirectory(src/visual)
add_subdirectory(api)
