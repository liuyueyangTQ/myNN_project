# 设置 net_test 测试文件
add_executable(net_test 
    ${TOOLS_DIR}/netService/net_test.cpp
)
add_netService_support(net_test
    DEFINES
        USE_DEBUG
    LINKS
        netService_libs
)

# 设置 json_test 测试文件
add_executable(json_test 
    ${TOOLS_DIR}/json/json_test.cpp
)
add_json_support(json_test
    DEFINES
        USE_DEBUG
    LINKS
        json_net_libs
    FILE_DIR
        "${TOOLS_DIR}/json" # 要传入路径
)

# 设置 json_test 测试文件
add_executable(webserver_test 
    ${TOOLS_DIR}/json/json_tool.cpp
    ${TOOLS_DIR}/webserver/webserver_test.cpp
    ${TOOLS_DIR}/webserver/webserver.cpp
)
add_web_support(webserver_test)
add_json_support(webserver_test
    DEFINES
        USE_DEBUG
    FILE_DIR
        "${TOOLS_DIR}/webserver" # 要传入路径
)

# 设置 Protobuf 测试文件
add_executable(proto_test 
    ${TOOLS_DIR}/protobuf/proto_test.cpp
)
add_protobuf_support(proto_test
    INCLUDES 
        ${TOOLS_DIR}/protobuf
    LINKS
        proto_libs
)
add_nn_support(proto_test
    MAIN_DIR    
        ${CORE_DIR}
    DEFINES
        USE_DEBUG 
    LINKS
        math_libs
)

add_subdirectory(${TOOLS_DIR})
add_subdirectory(${CORE_DIR})