cmake_minimum_required(VERSION 3.10)
project(Metrix_test)

# 设置C++标准
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
# 1. 先设置构建类型为 Debug（关键！CMake 会自动适配 Debug 模式的默认编译选项）
set(CMAKE_BUILD_TYPE Debug CACHE STRING "Build type" FORCE)

# 2. 强制为 C/C++ 设置 Debug 模式的编译选项（覆盖默认，确保 -g -O0 生效）
# 针对 C++ 文件
set(CMAKE_CXX_FLAGS_DEBUG "${CMAKE_CXX_FLAGS_DEBUG} -g -O0")
# 如果项目包含 C 文件，补充这行
set(CMAKE_C_FLAGS_DEBUG "${CMAKE_C_FLAGS_DEBUG} -g -O0")

# 对于MinGW，需要设置正确的编译器标志
if(MINGW)
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -static-libgcc -static-libstdc++")
endif()

# 编译阶段：添加-flto
if(CMAKE_CXX_COMPILER_ID MATCHES "GNU|Clang")
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -O3 -flto -march=native")
    # 链接阶段：添加-flto
    set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} -flto")
    set(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} -flto")
endif()

add_executable(metrix_test
    ${CMAKE_SOURCE_DIR}/test/metrix_test.cpp
    ${CMAKE_SOURCE_DIR}/math/metrix.cpp
    ${CMAKE_SOURCE_DIR}/utils/buffer_util.cpp
    # src/glad.c
    # src/Shader.cpp
)
target_include_directories(metrix_test PRIVATE
    ${CMAKE_SOURCE_DIR}/math
    ${CMAKE_SOURCE_DIR}/tensor
    ${CMAKE_SOURCE_DIR}/nn
    ${CMAKE_SOURCE_DIR}/utils    
)


add_executable(tensor_test
    ${CMAKE_SOURCE_DIR}/test/tensor_test.cpp
    ${CMAKE_SOURCE_DIR}/math/metrix.cpp
    ${CMAKE_SOURCE_DIR}/tensor/tensor.cpp
    ${CMAKE_SOURCE_DIR}/utils/buffer_util.cpp
    # src/glad.c
    # src/Shader.cpp
)
target_include_directories(tensor_test PRIVATE
    ${CMAKE_SOURCE_DIR}/math
    ${CMAKE_SOURCE_DIR}/tensor
    ${CMAKE_SOURCE_DIR}/nn
    ${CMAKE_SOURCE_DIR}/utils 
)
target_compile_definitions(tensor_test
    PRIVATE       # 仅当前目标（tensor_test）的源文件可见
        USE_DEBUG 
)


add_executable(nn_test
    ${CMAKE_SOURCE_DIR}/test/nn_test.cpp
    ${CMAKE_SOURCE_DIR}/math/metrix.cpp
    ${CMAKE_SOURCE_DIR}/tensor/tensor.cpp
    ${CMAKE_SOURCE_DIR}/nn/model.cpp
    ${CMAKE_SOURCE_DIR}/thread/threadpool.cpp
    ${CMAKE_SOURCE_DIR}/utils/buffer_util.cpp
    # src/glad.c
    # src/Shader.cpp
)
target_include_directories(nn_test PRIVATE
    ${CMAKE_SOURCE_DIR}/math
    ${CMAKE_SOURCE_DIR}/tensor
    ${CMAKE_SOURCE_DIR}/nn
    ${CMAKE_SOURCE_DIR}/thread
    ${CMAKE_SOURCE_DIR}/utils
)
target_compile_definitions(nn_test
    PRIVATE       # 仅当前目标（tensor_test）的源文件可见
        USE_DEBUG 
        USE_THREAD
)


add_executable(thread_test
    ${CMAKE_SOURCE_DIR}/test/thread_test.cpp
    ${CMAKE_SOURCE_DIR}/math/metrix.cpp
    ${CMAKE_SOURCE_DIR}/tensor/tensor.cpp
    ${CMAKE_SOURCE_DIR}/nn/model.cpp
    ${CMAKE_SOURCE_DIR}/thread/threadpool.cpp
    ${CMAKE_SOURCE_DIR}/utils/buffer_util.cpp
    # src/glad.c
    # src/Shader.cpp
)
target_include_directories(thread_test PRIVATE
    ${CMAKE_SOURCE_DIR}/math
    ${CMAKE_SOURCE_DIR}/tensor
    ${CMAKE_SOURCE_DIR}/nn
    ${CMAKE_SOURCE_DIR}/thread
    ${CMAKE_SOURCE_DIR}/utils 
)
target_compile_definitions(thread_test
    PRIVATE       # 仅当前目标（tensor_test）的源文件可见
        USE_DEBUG 
        USE_THREAD
)


add_executable(dtensor_test
    ${CMAKE_SOURCE_DIR}/test/dtensor_test.cpp
    ${CMAKE_SOURCE_DIR}/math/metrix.cpp
    ${CMAKE_SOURCE_DIR}/tensor/dtensor.cpp
    ${CMAKE_SOURCE_DIR}/tensor/ops.cpp
    #${CMAKE_SOURCE_DIR}/nn/model.cpp
    # ${CMAKE_SOURCE_DIR}/thread/threadpool.cpp
    # ${CMAKE_SOURCE_DIR}/utils/buffer_util.cpp
    # src/glad.c
    # src/Shader.cpp
)
target_include_directories(dtensor_test PRIVATE
    ${CMAKE_SOURCE_DIR}/math
    ${CMAKE_SOURCE_DIR}/tensor
    ${CMAKE_SOURCE_DIR}/nn
    ${CMAKE_SOURCE_DIR}/thread
    ${CMAKE_SOURCE_DIR}/utils 
)
target_compile_definitions(dtensor_test
    PRIVATE       # 仅当前目标（dtensor_test）的源文件可见
        USE_DEBUG 
        USE_THREAD
)

add_executable(dnn_test
    ${CMAKE_SOURCE_DIR}/test/dnn_test.cpp
    ${CMAKE_SOURCE_DIR}/math/metrix.cpp
    ${CMAKE_SOURCE_DIR}/tensor/dtensor.cpp
    ${CMAKE_SOURCE_DIR}/tensor/ops.cpp
    ${CMAKE_SOURCE_DIR}/nn/nn.cpp
)
target_include_directories(dnn_test PRIVATE
    ${CMAKE_SOURCE_DIR}/math
    ${CMAKE_SOURCE_DIR}/tensor
    ${CMAKE_SOURCE_DIR}/nn
    ${CMAKE_SOURCE_DIR}/thread
    ${CMAKE_SOURCE_DIR}/utils 
)
target_compile_definitions(dnn_test
    PRIVATE       # 仅当前目标（dtensor_test）的源文件可见
        USE_DEBUG 
        USE_THREAD
        DEBUG_ASSERT_H
)


#set(PARENT_SOURCE_DIR "${CMAKE_SOURCE_DIR}/../../") # 获取父目录路径
#cmake_path(REAL_PATH ${PARENT_SOURCE_DIR} OUTPUT_VARIABLE PARENT_SOURCE_DIR)

get_filename_component(PARENT_SOURCE_DIR ${CMAKE_SOURCE_DIR} DIRECTORY)
get_filename_component(PARENT_SOURCE_DIR ${PARENT_SOURCE_DIR} DIRECTORY)
add_executable(draw_nn_test
    ${CMAKE_SOURCE_DIR}/test/draw_nn_test.cpp
    ${CMAKE_SOURCE_DIR}/math/metrix.cpp
    ${CMAKE_SOURCE_DIR}/tensor/tensor.cpp
    ${CMAKE_SOURCE_DIR}/nn/model.cpp
    ${CMAKE_SOURCE_DIR}/thread/threadpool.cpp
    ${PARENT_SOURCE_DIR}/src/visual/buttons.cpp
    ${CMAKE_SOURCE_DIR}/utils/buffer_util.cpp
    # src/glad.c
    # src/Shader.cpp
)
target_include_directories(draw_nn_test PRIVATE
    ${CMAKE_SOURCE_DIR}/math
    ${CMAKE_SOURCE_DIR}/tensor
    ${CMAKE_SOURCE_DIR}/nn
    ${CMAKE_SOURCE_DIR}/thread
    ${PARENT_SOURCE_DIR}/inc
    ${CMAKE_SOURCE_DIR}/utils
)

set(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} "${PARENT_SOURCE_DIR}/cmake/")
message(STATUS "PARENT_SOURCE_DIR is: ${PARENT_SOURCE_DIR}")
# 2. 引入封装的OpenGL构建逻辑
include(openGLhelper)

add_opengl_support(draw_nn_test)



target_compile_definitions(draw_nn_test
    PRIVATE       # 仅当前目标（tensor_test）的源文件可见
        USE_DEBUG 
    #    USE_THREAD
)